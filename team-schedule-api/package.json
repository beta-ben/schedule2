{
	"name": "team-schedule-api",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --persist-to .wrangler/state --log-level warn",
		"start": "wrangler dev"
	},
	"devDependencies": {
    "wrangler": "^4.35.0",
		"@cloudflare/workers-types": "^4.20240821.0"
	}
}
