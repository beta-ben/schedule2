{
  "name": "team-schedule-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "setup": "npm install && cd team-schedule-api && npm install",
    "deploy": "wrangler deploy",
    "dev": "DEV_MODE=1 wrangler dev --port 8787 --persist-to .wrangler/state --log-level warn",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.35.0",
    "@cloudflare/workers-types": "^4.20240821.0"
  }
}
